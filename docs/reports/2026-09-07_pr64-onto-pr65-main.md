# 2026-09-07 — PR #64 onto current main (PR #65)

## Goal

Integrate `cursor/armor-crouch-swing-flash-3f93` with `origin/main` after PR #65 merged. Keep PR #65 food/potions/gameplay. Keep PR #64 crouch, bright hurt flash, air swing. Keep PR #62 claim wires. Delete PR #64 eat/drink pose and armor overlay.

## Result

Merged `origin/main` (`bcc35df`) into the PR #64 branch. One eat/drink system remains: PR #65 `localFoodUse` + `FirstPersonRenderer.applyEatPose`.

## Audit of PR #65

- **Food/potions:** captured hotbar slot + `commandSeq` boundary; Apple/GoldenApple/Potion Regeneration/Invisibility; release/slot/death/reconnect cancel; bottle return and overflow drop; client presentation never consumes.
- **Eat/drink animation:** existing `applyEatPose` on first-person; `foodUseTicks` / `foodUseProgress` drive arm pose in `PlayerVisualAnimator`; remotes already receive `foodUseProgress`.
- **movementDuringItemUse:** shared 0.2 bow/sword slowdown for SP, prediction, server; no double apply.
- **Claims PvP:** victim + real attacker `pvp`; melee/Arrow/FireArrow; wilderness allowed; unknown attacker → `mob-damage`.
- **Cobweb / fallDistance:** no normal jump in web; vertical damping; support-based fall reset; real fall damage kept.
- **TNT:** snapshot-authoritative fuse; interpolation calls existing pulse; no client detonation.

Also on current main (pre-#65): arrow PvP `attackerId` + FireArrow pickup (`bf2ed08`).

## Implemented

- Merge `origin/main` (not rebase/force-push).
- Deleted `src/rendering/heldItemEatPose.ts`, `applyEatDrinkHeldItemPose`, `ownFoodUseProgress` / `localFoodUseProgress`.
- Restored `FirstPersonRenderer.applyEatPose`.
- `PlayerVisual` no longer bakes a third-person item bobble; crouch hierarchy and mob-equivalent hurt tint stay.
- Air swing still sends `{ type: 'attack' }` on every discrete online click.
- Claim wires still 3px + depth test/write.
- No `playerArmor.ts`.

## Changed files

Merge plus strip of PR #64 eat/drink helper; docs union.

## Architecture decisions

- PR #65 consumable lifecycle and first-person pose are canonical.
- PR #64 third-person held-item bobble was a duplicate and is gone.
- Animator `foodUseProgress` arm pose is pre-existing presentation, not the deleted helper.

## Tests

- `npm run typecheck` / `typecheck:client` / `typecheck:server` / `typecheck:sim` PASS
- `npm run check:boundaries` PASS
- `npm run test:sim` **42/42 PASS**
- Directed visual/consumable/claims/physics/TNT/PvP: **147/147 PASS** (`player-visual-animation`, `player-skins`, `remote-action-presentation`, `remote-player-view`, `remote-presentation`, `fire-overlay-hurt`, `item-rendering`, `online-consumable-use`, `player-physics`, `claim-boundary`, `claims`, `player-actions`, `use-interaction`, `redstone`, `fire-arrow-and-fire`)
- Extra: `potion-effects-hud`, `online-networking-v2-contract`, `anarchy-plugins`, `client-server-lockstep`, `combat`, `mob-hurt-flash` — **120/120 PASS** across 7 files
- `npm run test:server -- --maxWorkers=2`: **266/267 PASS**; only `tick-load-flight` `setView` maxMs 111–130 > 80
- Same `tick-load-flight` gate fails on current `origin/main` (`bcc35df`): maxMs 82–111 > 80. Threshold not changed.
- `npm run build` PASS (`tsc --noEmit && vite build`)

## Visual QA

Automated hierarchy (upperBody hip pivot, `bodyYOffset`/`bodyZOffset` = 0), mob-equivalent hurt tint, air miss `swingSeq`, PR #65 consumable/cobweb/TNT/PvP, claim 3px + depth. Live two-client crouch/eat/flash/claim occlusion remains owner QA on a desktop; this VM is SwiftShader ~4 FPS.

## Known issues

- `tick-load-flight` wall-clock on this VM (also on current main).
- Owner live QA of the union.

## Deferred

New armor renderer.

## Next work

Merge this branch into `main` after the recorded audit; owner two-client QA.

## Git

Branch `cursor/armor-crouch-swing-flash-3f93` merged `origin/main` @ `bcc35df` (PR #65). Tip at tests: `b5796fa`.
